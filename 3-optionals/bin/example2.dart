void main(List<String> args) {
  /// If we works with with null without datatype best solution to go is dynamic
  const someValue = null;

  /// dynamic someValue
  /// null by it self is dynamic and not optional for a datatype
  print(someValue);
}
