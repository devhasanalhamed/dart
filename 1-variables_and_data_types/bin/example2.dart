void main(List<String> args) {
  /// kind of similar to const but also very different
  /// can't reassign value to the variable but can change in the value itself.
  /// [final] is the keyword to identify final.

  final int age = 30;
  print(age);

  /// age = 32; The final variable 'age' can only be set once.
  /// Try making 'age' non-final.
}
