void main(List<String> args) {
  /// data that you specify while writing
  /// the code it well held inside the memory
  /// and con't be modified at all after created.
  /// [const] is the keyword to identify constant.
  /// any thing can assigned value to is a variable.
  const name = 'foo';
  print(name);

  /// name = 'boo'; Constant variables can't be assigned a value.
  /// Try removing the assignment, or remove the modifier 'const' from the variable.
  /// There are two types of change 1- assign new value 2- change the value it self
  /// every modification to a constant variable are not allowed
}
